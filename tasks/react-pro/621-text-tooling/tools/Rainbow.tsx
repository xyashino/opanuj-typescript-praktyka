// function rainbowText(text: string) {
//   return text.split('').map((char: string, index: number) => {
//     const hue = Math.floor((index / text.length) * 360);
//     return (
//       <span key={index} style={{ color: `hsl(${hue}, 80%, 50%)` }}>
//         {char}
//       </span>
//     );
//   });
// }

export default function withRainbow() {}
